---
- name: Configuring the CH
  # hosts: hostvars[host]
  hosts: all
  # delegate_to: localhost
  # connection: local  
  # user: root
  # # become: true
  # roles:
  #   - role: httpd
  #   # - firewalld
  #   # - selinux

  tasks:
    - name: check hostname
      command: hostname
